FROM node:latest

WORKDIR /home/node/app

COPY . .

RUN npm i

ENV NODE_ENV=production
ENV PORT=5005
ENV MORGAN_FORMAT=tiny
ENV NAMESPACE=default
ENV PROCESS_INTERVAL=3000
ENV RANDOM_PERCENT=0

ENTRYPOINT ["npm", "run", "start"]